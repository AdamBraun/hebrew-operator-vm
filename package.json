{
  "name": "hebrew-operator-vm",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "fast-check": "^3.23.2",
    "typescript": "^5.4.0",
    "vitest": "^1.5.0"
  }
}
