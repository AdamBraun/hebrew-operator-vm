# ה — Export (implemented)

Status: implemented in reference interpreter.

## Signature

- Arity: req 1, opt 0.
- Operand kinds: any.
- Selection precedence: default (see `spec/60-VM.md`).
- Distinctness/reflexive: default (see `registry/letters.yaml`).

## Select

Selects the current focus handle `F`.

## Bound

Marks the construction envelope as `policy=final`.

## Seal

Allocates an `ArtifactHandle` derived from the focus and returns it as `h_out`.

## Obligations

None.

## Macro form

- Uses `Y` to pin the exported object and a `Seal`-time finalization in `Δ_ה`.

## Tests

- `/tests/letters/README.md#he`
