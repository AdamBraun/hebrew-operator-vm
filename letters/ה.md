# ה — Backed head with detached adjunct leg

Status: spec synced with `mission-statement.md`. Implementation may differ.

## Definition

- **Select:** current construct `X`. If no construct exists yet in the word, select word-entry ambient `W₀`.
- **Bound:**
  1. `h := alloc()`
  2. add `head_of(h, X)`
  3. add `carry(X, h)`
  4. add `supp(h, X)`
  5. `ℓ := alloc()`
  6. add `cont(h, ℓ)`
  7. add `carry(h, ℓ)`
  8. add `supp(ℓ, h)`
  9. export `ℓ` as an adjunct of `h`
- **Seal:** set `F := h`.

## Interpretation

A resolved head that also issues a detached, settled act-line.

## Family

- `ר`: head only, unresolved
- `ד`: head only, resolved
- `ק`: head + detached leg, unresolved
- `ה`: head + detached leg, resolved

## Signature

- Arity: req 1, opt 0.
- Operand kinds: any.
- Selection precedence: default (see `spec/60-VM.md`).
- Distinctness/reflexive: default (see `registry/letters.yaml`).

## Tests

- /tests/letters/04_letters/he.contract.test.ts
- /tests/letters/04_letters/he.behavior.test.ts
