# ז — Exported resolved port (focus stays)

Status: target behavior for the current reference runtime. Code sync may lag.

## Definition

Unary. Same materialized graph edges as ן, but focus does not advance.

- **Select:** current focus `(F)`.
- **Bound:** allocate port `p := alloc()`.
- **Graph edges emitted:**
  1. `cont(F, p)`
  2. `carry(F, p)`
  3. `supp(p, F)`
- **Other state changes:** set `edge_mode(p) := committed`; set `envelope.data_flow := SNAPSHOT`; export `p` to `K`.
- **Seal:** keep `F` unchanged.

Non-normative note: committedness here is structural rather than policy-gated. The port is treated as a sealed witness by its edge mode and envelope, not by `policy`.

## Signature

- Arity: req 1, opt 0.
- Operand kinds: any.
- Selection precedence: default (see `spec/60-VM.md`).
- Distinctness/reflexive: default (see `registry/letters.yaml`).

## Tests

- /tests/letters/04_letters/zayin.contract.test.ts
- /tests/letters/04_letters/zayin.behavior.test.ts
