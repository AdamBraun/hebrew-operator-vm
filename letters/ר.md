# ר — Bare head / unresolved head exposure

Status: synced to the current reference runtime.

## Definition

- **Select:** current construct/source `X`. On word-entry baseline, use word-entry focus `W₀`.
- **Bound:** allocate head `h`.
- **Graph edges emitted:**
  1. `head_of(h, X)`
  2. `cont(X, h)`
  3. `carry(X, h)`
- **Seal:** set `F := h`.

## Signature

- Arity: req 1, opt 0.
- Operand kinds: any.
- Selection precedence: default (see `spec/60-VM.md`).
- Distinctness/reflexive: default (see `registry/letters.yaml`).

## Tests

- /tests/letters/04_letters/resh.contract.test.ts
- /tests/letters/04_letters/resh.behavior.test.ts
