# ס — Support Close (implemented)

Status: implemented in reference interpreter.

## Signature

- Arity: req 1, opt 0.
- Operand kinds: any.
- Selection precedence: default (see `spec/60-VM.md`).
- Distinctness/reflexive: default (see `registry/letters.yaml`).

## Select

Selects the current focus handle `F`.

## Bound

Marks the focus handle as `policy=framed_lock` and stable.

## Seal

If a `SUPPORT` obligation is on top of `OStack_word` and is continuation-reachable from `F`, it is popped and logged as a support event.

## Obligations

- Closes: `SUPPORT`

## Macro form

- Uses `Y` to pin a stabilized hull and updates envelope policies.

## Tests

- `/tests/letters/README.md#samekh`
