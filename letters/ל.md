# ל — Hold and Step Past

Status: spec synced with `mission-statement.md`. Implementation may differ.

See also: [Hold Family Topology](hold-family-topology.md).

## Definition (from mission-statement)

- **Select:** current focus `(F)`.
- **Bound:**
  1. allocate resolved hold `h := alloc()`
  2. allocate exterior successor `o := alloc()`
  3. add `cont(F, h)`
  4. add `carry(F, h)`
  5. add `supp(h, F)`
  6. add `cont(h, o)`
- **Seal:** set focus to `o`.
- **Non-effects:** do **not** add `carry(h, o)`, `supp(o, h)`, or any boundary/enclosure record.
- **Shape note:** ל is the step-past member of the כ-based hold family: the hold is resolved first, then focus advances beyond it.

## Signature

- Arity: req 1, opt 0.
- Operand kinds: any.
- Selection precedence: default (see `spec/60-VM.md`).
- Distinctness/reflexive: default (see `registry/letters.yaml`).

## Tests

- /tests/letters/04_letters/lamed.contract.test.ts
- /tests/letters/04_letters/lamed.behavior.test.ts
