# מ — Mem Open (implemented)

Status: implemented in reference interpreter.

## Signature

- Arity: req 1, opt 0.
- Operand kinds: any.
- Selection precedence: default (see `spec/60-VM.md`).
- Distinctness/reflexive: default (see `registry/letters.yaml`).

## Select

Selects the current focus handle `F`.

## Bound

Allocates a new mem-zone handle `Z` associated to the focus.

## Seal

Pushes a `MEM_ZONE` obligation onto `OStack_word` and returns the original focus handle.

## Obligations

- Opens: `MEM_ZONE`

## Macro form

- Uses `Y` to seed a zone and defers closure/export to a later seal or boundary.

## Tests

- `/tests/letters/README.md#mem`
