# ו — Connector (implemented)

Status: implemented in reference interpreter.

## Signature

- Arity: req 2, opt 0.
- Operand kinds: any.
- Selection precedence: default (see `spec/60-VM.md`).
- Distinctness/reflexive: default (see `registry/letters.yaml`).

## Select

Selects two operands `(from, to)` using the default selection order.

## Bound

Creates a construction linking `from → to`.

## Seal

Allocates a structured link handle labeled `vav` and records a link `from → to` with label `vav`.

## Obligations

None.

## Macro form

- `V` is the primary primitive; placement `Δ_ו` binds the connection into a handle.

## Tests

- `/tests/letters/README.md#vav`
