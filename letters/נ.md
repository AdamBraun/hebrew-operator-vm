# נ — Unresolved continuation

Status: synced to the current reference runtime.

## Definition

- **Select:** current focus `(F)`.
- **Bound:** allocate successor `F^{+} := alloc()`.
- **Graph edges emitted:**
  1. `cont(F, F^{+})`
  2. `carry(F, F^{+})`
- **Seal:** set `F := F^{+}`.
- **Note:** current code records the unresolved carry in the graph only; it does not push a separate SUPPORT obligation object.

## Signature

- Arity: req 1, opt 0.
- Operand kinds: any.
- Selection precedence: default (see `spec/60-VM.md`).
- Distinctness/reflexive: default (see `registry/letters.yaml`).

## Tests

- `/tests/letters/README.md#nun`
