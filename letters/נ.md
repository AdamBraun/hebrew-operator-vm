# נ — Support Open (implemented)

Status: implemented in reference interpreter.

## Signature

- Arity: req 1, opt 0.
- Operand kinds: any.
- Selection precedence: default (see `spec/60-VM.md`).
- Distinctness/reflexive: default (see `registry/letters.yaml`).

## Select

Selects the current focus handle `F`.

## Bound

Allocates a successor scope `F⁺`, records a continuation `cont(F, F⁺)`, and carries forward the effective bundle.

## Seal

Pushes a `SUPPORT` obligation onto `OStack_word` and returns `F⁺` as `h_out`.

## Obligations

- Opens: `SUPPORT`

## Macro form

- Uses `V` to extend continuation and `Y` to seed the successor.

## Tests

- `/tests/letters/README.md#nun`
