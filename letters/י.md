# י — Seed (implemented)

Status: implemented in reference interpreter.

## Signature

- Arity: req 1, opt 0.
- Operand kinds: any.
- Selection precedence: default (see `spec/60-VM.md`).
- Distinctness/reflexive: default (see `registry/letters.yaml`).

## Select

Selects the current focus handle `F`.

## Bound

No special bound behavior; the construction base is `F`.

## Seal

Allocates a new seeded handle associated to the focus and returns it as `h_out`.

## Obligations

None.

## Macro form

- `Y` primitive with placement `Δ_י` that seeds a new addressable handle.

## Tests

- `/tests/letters/README.md#yod`
